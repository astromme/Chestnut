parallel real set_to(window element, real number) {
  return number;
}

real2d data[10, 10] = :set_to(data, 1.1);
real reduced = :reduce(data);

print("Array of floats was:");
print(data);
print("Reduced value is %s", reduced);
