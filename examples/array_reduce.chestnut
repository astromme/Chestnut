parallel set_to_1(window) {
  return 1;
}

real2d data(10, 10);
real reduced;
data = :map(data, set_to_1);
 
reduced = :reduce(data);
print("Array of floats was:");
:print(data);
print("Reduced value is %s", reduced);
