parallel Real set_to(Real number) {
  return number;
}

Real2d data[10, 10] = :set_to(1.1);
Real reduced = :reduce(data);

print("Array of floats was:");
print(data);
print("Reduced value is %s", reduced);
