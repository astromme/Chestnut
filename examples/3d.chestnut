IntArray3d grid[5, 2, 2];

foreach element in grid
  element = element.x;
end

print('::x positions');
print('%s', grid);

foreach element in grid
  element = element.y;
end

print('::y positions');
print('%s', grid);

foreach element in grid
  element = element.z;
end

print('::z positions');
print('%s', grid);

