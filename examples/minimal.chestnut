int2d unsorted(10, 5);
unsorted = :random(80, 100);

int2d sorted(10, 5);
sorted = :sort(unsorted);

int reduced;
reduced = :reduce(sorted);

:print(unsorted);
:print(sorted);
print("Reduced = %s", reduced);
