IntArray2d unsorted[10, 5]; 

foreach element! in unsorted
  element = random();
end

IntArray2d sorted[10, 5] = sort(unsorted);

Int reduced = reduce(unsorted);

print(unsorted);
print(sorted);
print("Reduced = %s", reduced);
