// sample Chestnut file to bootstrap the build process
// and give an example of Chestnut code
float data 10 10 foreach (value = rand/RAND_MAX);
float reduced scalar;

data = map(*,2,data);
reduced = reduce(+, data);
print data;
print reduced;
data = map(/, 2, data);
